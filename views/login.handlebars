{{!-- login.handlebars --}}

<div class="login-container d-flex justify-content-center align-items-center" style="min-height: 100vh; background: #f0f4f8;">
    <div class="login-box p-4 border rounded bg-white shadow-lg" style="max-width: 400px; width: 100%; position: relative;">
        <!-- Logo Image -->
        <div class="text-center">
            <img src="/img/logo.png" alt="Landslide App" class="img-fluid" style="max-width: 80px;">
        </div>
        
        <!-- Title -->
        <h2 class="text-center mb-4 text-primary">Login</h2>
        
        <!-- Login form -->
        <form action="/login" method="POST">
            <!-- Username or Email field -->
            <div class="mb-3">
                <label for="identifier" class="form-label">Username or Email</label>
                <input type="text" class="form-control" id="identifier" name="identifier" required placeholder="Enter your username or email" autofocus>
            </div>
            
            <!-- Password field -->
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password" name="password" required placeholder="Enter your password">
            </div>

            <!-- Submit button -->
            <button type="submit" class="btn btn-primary w-100 py-2 mt-3">Login</button>

            <!-- Optional: Forgot password link -->
            <div class="mt-3 text-center">
                <a href="/forgot-password" class="text-decoration-none text-muted">Forgot your password?</a>
            </div>
        </form>
    </div>
</div>

<style>

/* Custom styling for the login page */
.login-container {
    min-height: 100vh;
    background: #f0f4f8; /* Light background color */
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
}

/* Login box styling */
.login-box {
    max-width: 400px;
    width: 100%;
    padding: 2rem;
    background-color: #ffffff;
    padding: 40px;
    border-radius: 12px !important;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
}

/* Heading styling */
.login-box h2 {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    font-size: 1.75rem;
    font-weight: 600;
    color: #007bff; /* Primary color for text */
    margin-bottom: 1.5rem;
}

/* Logo image styling */
.login-box img {
    width: 80px;
    height: auto;
    max-width: 100%;
    margin-bottom: 20px;
}

/* Input fields styling */
.form-control {
    padding: 0.8rem 1.2rem;
    border-radius: 8px;
    font-size: 1rem;
    border: 1px solid #ced4da;
    box-shadow: none;
    transition: border-color 0.3s ease-in-out;
}

/* Focus effect on inputs */
.form-control:focus {
    border-color: #007bff; /* Highlight input field on focus */
    box-shadow: 0 0 0 0.2rem rgba(38, 143, 255, 0.25); /* Subtle focus shadow */
}

/* Submit button styling */
.btn-primary {
    background-color: #007bff;
    border-color: #007bff;
    border-radius: 8px;
    padding: 0.8rem;
    font-size: 1.1rem;
    font-weight: 600;
    transition: background-color 0.2s ease;
}

.btn-primary:hover {
    background-color: #0056b3;
    border-color: #0056b3;
}

/* Forgot password link styling */
.text-muted {
    color: #6c757d !important; /* Subtle muted color */
}

.text-muted:hover {
    color: #007bff !important; /* Highlight on hover */
}

/* Optional: Add a subtle background gradient */
body {
    background: #f0f4f8; /* Soft background gradient */
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Responsive design */
@media (max-width: 768px) {
    .login-box {
        padding: 20px;
        max-width: 90%;
    }
}

</style>

